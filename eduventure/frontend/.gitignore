# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories
node_modules/

# Production build directories
dist/
dist-ssr/

# Local environment files (avoid committing secrets)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/
!.vscode/extensions.json
.idea/
.DS_Store

# OS generated files
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local build or temp files
*.local

# Cache and temp folders
.cache/
.tmp/

# Optional: Lockfiles, only commit those you use for consistency
# If using pnpm, commit pnpm-lock.yaml; for yarn yarn.lock; npm package-lock.json
# Uncomment if needed:
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
